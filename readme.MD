### Simulador de control de redundancia vertical y longitudinal (VRC - LRC)

para ejecutar el codigo en local:
1) instalamos los paquetes

```
npm i
```
2) corremos el servidor
```
npm run dev
```

para compilar y generar el build: 

```
npm run build
```